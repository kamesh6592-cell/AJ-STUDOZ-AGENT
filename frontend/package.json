{
  "name": "aj-personal-agent",
  "version": "1.0.0",
  "description": "AI Website Studio by AJ STUDIOZ - Cloudflare Pages",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --local --port 8788",
    "deploy": "wrangler pages publish . --project-name=aj-personal-agent",
    "tail": "wrangler pages deployment tail",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "hono": "^3.12.8"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
